{
  "name": "prototype-usfm",
  "version": "0.0.0",
  "private": true,
  "description": "THROWAWAY — pressure-test Phase-1 schema.dbml against real USFM. Delete after the verdict is folded into docs.",
  "scripts": {
    "ingest": "tsx src/main.ts",
    "repl": "tsx src/main.ts --repl"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
