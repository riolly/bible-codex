{
  "name": "bible-codex-design",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Standing design harness — browser CanvasKit over the PRODUCTION engine + draw layer. Feature dirs are throwaway (death ritual in README.md); the harness stays.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@shopify/react-native-skia": "2.6.2",
    "canvaskit-wasm": "0.41.0",
    "react": "19.2.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
