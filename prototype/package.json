{
  "name": "bible-codex-prototype",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "THROWAWAY prototype — proves Skia can render beautiful literary scripture + smooth scroll. Not production. See README.md.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "canvaskit-wasm": "^0.39.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
